### Example .env file for examples/send_example.py

# Browser cookie string copied from DevTools ('name=value; name2=value2; ...')
COOKIE_STRING='EXAMPLE_COOKIE=abcd; OWAAuth=...'

# Comma-separated recipient addresses
TO_ADDRS=recipient1@amazon.com,recipient2@amazon.com,recipient3@amazon.com

# Optional: CC and BCC recipients (comma-separated)
# CC_ADDRS=cc1@amazon.com,cc2@amazon.com
# BCC_ADDRS=bcc1@amazon.com

# Email subject and HTML body
SUBJECT=Test email from script
BODY=<p>Hello</p><p>Thanks,</p><p>Firstname</p>

## Create/Update action headers extracted from browser cURLs
# CreateItem (from your cURL)
CREATE_ACTION_ID=-1
CREATE_ACTION_NAME=CreateMessageForCompose

# # UpdateItem (from your cURL)
# UPDATE_ACTION_ID=-2
# UPDATE_ACTION_NAME=UpdateMessageForComposeSend

## Cognito Authentication (for Self Intro API and Job Search API)
# Extract refresh token from browser localStorage (ONE-TIME setup)
# See REFRESH_TOKEN_GUIDE.md for detailed extraction instructions
# 
# Quick steps:
# 1. Open https://prod.aboutme.talent.amazon.dev in Chrome (logged in)
# 2. DevTools (F12) → Application → Local Storage → prod.aboutme.talent.amazon.dev
# 3. Find key containing 'refreshToken' and copy the value
# 
# This refresh token enables automatic token refresh for:
#   - get_jobs.py (job search API)
#   - get_employee_details.py (employee info API)
#   - internal_transfer_request_informational_automator_pipeline.py (Self Intro API)
# 
# Token lifetime: ~30 days (no hourly refresh needed!)
COGNITO_REFRESH_TOKEN='eyJjdHk...'

## AtoZ Federate/Passport Cookies (for get_job_details.py)
# IMPORTANT: AtoZ uses httpOnly cookies that CANNOT be accessed via JavaScript!
# These cookies expire every ~1 hour and CANNOT be auto-refreshed.
# 
# How to extract (Network Tab Method):
# 1. Open https://atoz.amazon.work in browser (logged in)
# 2. Open DevTools (F12) → Network tab
# 3. Refresh the page (Cmd+R / Ctrl+R)
# 4. Click on ANY request to atoz.amazon.work (pick one with Status: 200)
# 5. In the right panel, scroll to "Request Headers" section
# 6. Find the line that says "cookie:" (lowercase)
# 7. Copy the ENTIRE value after "cookie: " (right-click → Copy value)
# 8. Paste below:
JOB_DETAILS_COOKIE='paste entire cookie string from Network tab here'
#
# See extract_atoz_cookie.js for detailed visual guide
# Must re-extract every ~1 hour when cookie expires

# Optional: Override defaults
# COGNITO_CLIENT_ID='6hr71icfdda6n67uvvm3nvlu4d'
# COGNITO_IDENTITY_POOL_ID='us-west-2:74ab0fc1-ddcb-43b1-a90d-32fec0b92043'
# COGNITO_USER_POOL_PROVIDER='cognito-idp.us-west-2.amazonaws.com/us-west-2_fTk7zNMno'
JOBS_API_URL='https://api.prod.internal-transfer.talent.amazon.dev/v1/jobs/search?country=USA&jobCategory=Software%20Development&jobLevel=5&query=&sort=recent'
JOBS_PAGE_LIMIT=20

# Required for automated informational request pipeline
CANDIDATE_SUMMARY="I am a Software Engineer with 5 years of experience in backend development using Java and Python. I have a strong background in distributed systems and cloud computing. I'm looking for opportunities in software development roles that involve working with large-scale systems and machine learning."
REQUESTER_PEOPLE_SOFT_ID='your_peoplesoft_id_here'  # Your employee ID for API requests

# Google API credentials (required for Gemini/ADK in pipeline)
GOOGLE_API_KEY='key-for-google-api'  # Get from Google AI Studio
MODEL_FAST='gemini-2.5-flash'     # Fast model for simple tasks
MODEL_STRONG='gemini-2.5-pro'   # Strong model for complex generation
USER_ID=''
JOB_MATCH_RESULTS_FOLDER_NAME='output'
